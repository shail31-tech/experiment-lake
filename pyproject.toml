[project]
name = "experimentlake"
version = "0.1.0"
description = "Serverless A/B testing analytics platform"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
